{
  "name": "love-hui",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently 'pnpm:dev:*'",
    "dev:server": "pnpm run --dir server dev",
    "dev:api": "pnpm run --dir api start:dev",
    "dev:client": "pnpm run --dir client dev",
    "start": "concurrently 'pnpm:start:*'",
    "start:server": "pnpm run --dir server start",
    "start:client": "pnpm run --dir client start",
    "start:api": "pnpm run --dir api start",
    "stop": "concurrently 'pnpm:stop:*'",
    "stop:server": "pnpm run --dir server stop"
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
